import Layout from '../../components/docs-layout';
import { Button } from 'teneric-components';

export const meta = {
  title: 'Documentation',
};

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;

# Getting Started

Add beautiful notifications to your React app with [teneric-components](https://github.com/T-Every/teneric-components).

### Install with Yarn

```sh
yarn add teneric-components
```

### Install with NPM

```sh
npm install teneric-components
```

## Basic usage

```jsx
  <Button text=""/>
```
