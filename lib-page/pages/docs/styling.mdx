import Layout from '../../components/docs-layout';
import { Button } from 'teneric-components';

export const meta = {
  title: 'Styling',
};

export default ({ children }) => <Layout meta={meta}>{children}</Layout>;

# Styling

You can style your button

### ....

```jsx
<Button

/>
```